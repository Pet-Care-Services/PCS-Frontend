const minutesInDay = 60 * 24;

const tileInterval = 15;

const numberOfTiles = minutesInDay / tileInterval;

const availabilityComponentHeight = 550;

export {
  minutesInDay,
  tileInterval,
  numberOfTiles,
  availabilityComponentHeight,
};
