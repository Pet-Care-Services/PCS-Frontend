export default {
  activitiesTags: {
    backgroundColor: (theme) => theme.palette.secondary.dark,
  },
  animalsTags: {
    backgroundColor: (theme) => theme.palette.neutral.main,
  },
  root: {
    minHeight: 100,
    borderRadius: 25,
    p: 5,
  },
};
