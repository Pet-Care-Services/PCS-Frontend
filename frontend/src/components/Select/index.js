import React from 'react';
import FormikField from 'components/FormikField';
import SelectView from './view';

const Select = (props) => <FormikField Component={SelectView} {...props} />;

export default Select;
